info:
  name: get all events
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/v1/event/list
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
